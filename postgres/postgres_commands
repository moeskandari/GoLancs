CREATE TABLE operators(operator_code TEXT PRIMARY KEY, name TEXT, mode, TEXT);
CREATE TABLE rail_schedule(train_uid TEXT PRIMARY KEY, operator_code TEXT, schedule_start_date DATE, schedule_end_date DATE, days_run BYTEA, CONSTRAINT fk_operator FOREIGN KEY (operator_code) REFERENCES operators(operator_code));

