we are creating some setting for the setting page in profile page 
create the backend 
create the frontend 

it should include settings like:
- change password
    - input fields for current password, new password, and confirm new password
- dark and light mode: 
    - toggle switch for dark and light mode
    - when dark mode is selected, the background color of the profile page and other pages in the app should change to a dark color and the text color should change to a light color
- 3 options for font size (small, medium, large):
    - radio buttons for font size selection
    - when a font size is selected, the text size on the profile page and other pages in the app should change accordingly
- user doen't have to save the settings, it should be applied immediately when the user changes the setting
- user doesn't need to verify their email to change the settings, but they need to be logged in to access the settings page
- the settings should be stored in the database and should be associated with the user's account
- the settings should be retrieved from the database and applied when the user logs in
- the settings should be updated in the database when the user changes the settings
- the settings should be applied across all devices when the user logs in with their account
- the settings should be applied across all sessions when the user logs in with their account

