We need to initialise our filer page

This includes with heavy empahsis:
    - Uploading the created files to the provided repo 
    - Initialise back-end. 
    - Create front end.
    - make sure all the code only effects the filer page and does not affect any other pages on the website.
    - Once these features, and only these features are implemented, wait for the features to be signed off and tested so later it can be added to a new branch on the github repo known as UI 

For the front end:
    - make the fillter button intractive 
    - when the user presses it should re direct them to a new page called filter where they can select the options for the filter and then submit it to the back end to be processed.
    - the filter page should have a back button that takes the user back to the home page.
    - the filter page should have a submit button that submits the selected options to the back end to be processed.
    - the filters should bve splitted in three categories: 
         - on map
            - show bus stops
            - show train stations
            - show traffic conditions
         - direction
            - incloude walking in the route sugestions
            - include driving in the route suggestions
            - include busses in the route suggestions
            - include trains in the route suggestions 
         - route suggestions
            - show the fastest route
            - show the route with the least traffic
            - show the cheapest route
            - show the route with least number of changes
    - make the buttons for each of these filters interactive and make sure that when the user selects an option, it is highlighted to show that it has been selected.
    - make sure that when the user submits the selected options, they are sent to the back end to be processed and the user is redirected to a new page where they can see the results of their filter selections.
    - make sure that the results page only shows the results of the filter selections and does not show any other information that is not relevant to the filter selections.

Please make sure to follow all rules that we have set out and follow all instructions we have given to you especially the this includes section