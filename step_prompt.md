We need to initialise our application.

This includes:
    - Uploading the created files to the provided repo 
    - Initialise back-end. Keep this basic for now.
    - Create front end.
For the front end:
    - Create an interactive map on the home page - confined to the regions specified.
    - Two search bars, one for start, one for destination. 
    - Start location should defaultly show user's location (leave blank if unknown)
    - Three buttons on the bottom, one for filtering options, one for centering the map to their location, one for going to account page
        - These buttons do not need to have functionality yet.
    - We also want an interactive compass on top right corner. 

Once these features, and only these features are implemented, upload to the repo. 
Create a comprehensive README.md file to tell us how to run and understand the code. 
Also add this agent to the repo. 
